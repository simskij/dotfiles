[user]
name = Simon Aronsson
email = simme@arcticbit.se
signingkey = 8DA57A5FD341605B

[alias]
st = status
ci = commit
co = checkout
cp = cherry-pick
rc = rebase --continue
mc = merge --continue
df = diff
contributors = shortlog --summary --numbered
vomit = !git commit -m "$(curl -s http://whatthecommit.com/index.txt)"
	rewrite = config --global url.ssh://git@github.com/.insteadOf https://github.com/
	rm-rewrite = config --global --unset url.ssh://git@github.com/:.insteadOf

[color]
ui = true
grep = auto

[push]
default = current

[branch]
autosetuiprebase = always

[fetch]
prune = true

[commit]
verbose = true
gpgsign = true

[pull]
rebase = true
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
